import React from "react";
import { Helmet } from "react-helmet";
import Layout from "./Layout";

const Home = () => {
  return (
    <Layout>
      <Helmet title="Home" />
    </Layout>
  );
};

export default Home;
